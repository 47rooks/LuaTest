print(string.format("ball (x, y)=%s (%f, %f)", ball.Lname, ball.x, ball.y))
print(string.format("leftPaddle (x, y)=(%f, %f)", leftPaddle.x, leftPaddle.y))
print(string.format("rightPaddle (x, y)=(%f, %f)", rightPaddle.x, rightPaddle.y))
